#!/bin/sh

# Setup mccode-select and mccode-select-bundle
mccode-select-${FLAVOR}-${MCCODE_VERSION} --install \
    mccode-select ${FLAVOR}-${MCCODE_VERSION}

mccode-select-${FLAVOR}-${MCCODE_VERSION} --install \
    mccode-select-bundle ${FLAVOR}-${MCCODE_VERSION}

# Setup mcstas/mcxtrace including available tools
mccode-select-bundle --install \
    ${FLAVOR} \
    ${MCCODE_VERSION}
