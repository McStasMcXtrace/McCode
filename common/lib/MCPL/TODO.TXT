Output Comp
   - Userflag, incl command for mcpl (incl example for LOKI)
   - instrumentfil i mcpl header
   - defaults: double precision, default
   - (32 int) dump_file → unsigned (64 int) dump_file

Input Comp
xskip pdg!=2112, e_kin<,> threshold + summarize at end
(( particle==mcpl_read(..) && !canuse(particle)) 
  → ABSORB

