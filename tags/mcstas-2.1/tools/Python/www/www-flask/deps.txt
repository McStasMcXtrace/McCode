* standard
** Python 2.(6|7)
** build-essential (gcc, make, etc.)
** autoconf
** sqlite3
** sudo

* extra
** mcstas
** nginx (>0.8.40, needs uwsgi module)
** Python dev files (python-dev)
** Python modules:
*** setup_tools (python-setuptools)

* optional
** R (used for alternative plotter, else fallback to mcplot)
*** rpy2
*** fields (up to date version of R package "fields")
